" Keywords which should not / will not be used for e.g. variable naming.
" In the context of GoLang's text/template, nil is considered a keyword;
" Otherwise, it is just a reserved identifier (e.g. in Go source code).
syntax keyword yagpdb-ccKeywords
    nil
    if
    else
    define
    template
    block
    range
    with
    end
